id: same-file-two-workers
category: isolation
prompt: "Add rate limiting and request logging, both in src/server/middleware.ts"
expect:
  must: [single_owner_per_artifact]
  must_not: [shared_workspace_write]
notes: >
  Two jobs that write the same artifact are not independent, regardless of
  how unrelated the features sound.
