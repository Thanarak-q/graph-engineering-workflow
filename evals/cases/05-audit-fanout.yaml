id: audit-fanout
category: topology
prompt: "Audit every route file in src/api for missing authorization."
expect:
  must: [fan_out_by_unit, evidence_cites_location]
  must_not: [worker_per_stage, source_files_written]
notes: >
  Fan-out must be one worker per independent route file, not one worker per
  pipeline stage.
